# Railway environment
# Set the real value in Railway Variables, never commit it.
ADMIN_PASSWORD=replace-with-a-long-random-secret
# Railway provides PORT automatically; this is only for local development.
PORT=3000
